require "spec_helper"

RSpec.describe Ars::Denizen do
  it "has a version number" do
    expect(Ars::Denizen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
